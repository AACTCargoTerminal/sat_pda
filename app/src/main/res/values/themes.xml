<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- 앱 전체 테마 -->
    <style name="Theme.SAT_PDA" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:fontFamily">@font/pretendardgov</item>
        <item name="fontFamily">@font/pretendardgov</item>
    </style>

    <!-- 공통 EditText 스타일 (언더라인 제거 + 패딩 + 폰트 등 적용) -->
    <style name="Widget.MyApp.EditText" parent="Widget.AppCompat.EditText">
        <!-- 배경 제거 -->
        <item name="android:background">@null</item>
        <item name="android:backgroundTint">@null</item>
        <item name="backgroundTint">@null</item>

        <!-- 패딩 및 글꼴 -->
        <item name="android:padding">0dp</item>
        <item name="android:paddingStart">4dp</item>
        <item name="android:paddingEnd">4dp</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">#000000</item>
        <item name="android:fontFamily">@font/pretendardgov</item>
    </style>
</resources>